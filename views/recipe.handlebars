<section class="container">
  {{!-- <h3 class="Recipe-name">{{Recipe.name}}</h3>
  <h4>Opening Hour: {{format_time Recipe.starting_date}}</h4>
  <h4>Closing Hour: {{format_time Recipe.ending_date}}</h4> --}}
  <section class="flex-row justify-center">
    {{#each Recipe.images as |recipe|}}
    {{> recipe-details}}
    {{/each}}
  </section>
</section>